import { View, Text } from "react-native";

const Interview = () => {
    return (
        <View className="flex justify-center items-center">
            <Text className="text-5xl text-primary font-bold">Interview</Text>
        </View>
    );
};

export default Interview;
