import { View, Text } from "react-native";

const Dashboard = () => {
    return (
        <View className="flex justify-center items-center">
            <Text className="text-5xl text-primary font-bold">Dashboard</Text>
        </View>
    );
};

export default Dashboard;
